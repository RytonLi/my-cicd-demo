name: 我的第一个工作流

# 触发条件：当代码推送到main分支时
on:
  push:
    branches:
      - main

# 要执行的任务
jobs:
  hello:
    name: 打招呼
    runs-on: ubuntu-latest # 在 Ubuntu 最新版的跑者上运行

    steps:
      - name: 打印问候语
        run: echo "你好，CI/CD！"